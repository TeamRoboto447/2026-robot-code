# Vendor deps (`vendordeps/`)

CTRE Phoenix 6 (swerve/TalonFX), REVLib (SPARK/NEO), PathplannerLib, PhotonLib (vision), QuestNav, AdvantageKit (logging/replay, `annotationProcessor`'d for `@AutoLog`).
